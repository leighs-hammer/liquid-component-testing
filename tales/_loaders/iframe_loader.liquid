<!DOCTYPE html>
  {% include '_head' %}
  <body id="iframe_loader">
  
    {% include '_header' %}

    <div id="combinedStage">
      {% include '_sidebar' %}

      <div id="mainStage">
        {% if params.target %}
          <iframe 
            id="iframedComonent" 
            src="{% if params.type%}/{{params.type}}{% endif %}/{{params.taget}}"
          >
          
          </iframe>
        {% endif %}
      </div>

    <div>
    
    {% include '_foot' %}
  </body>
</html>
