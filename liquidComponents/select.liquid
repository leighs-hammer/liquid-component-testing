{% comment %} 
  // Renders a drop down for testing in 
{% endcomment %}
<h1>{{title}}</h1>
<select>
  {% for option in selectOptions %}
    <option value="{{option.val}}">{{option.name}}</option> 
  {% endfor %}
</select>